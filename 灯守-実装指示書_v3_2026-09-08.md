# 灯守（ひもり）v3 実装指示書 — 自分のデータだけで、毎日確実に賢くなるX運用エンジン

作成: 2026-09-08（設計: Claude Fable 5.1 ／ 実装担当: Claude Opus）
対象アカウント: **@crystalinsence**（柴垣道宏＝しばがき どうこう／Doko Shibagaki）
目標: **フォロワー30,000人**（1,000/1ヶ月 → 5,000/3ヶ月 → 30,000/6〜12ヶ月）
設計書（CEO向け・読み物）: https://claude.ai/code/artifact/11826217-fc51-4285-9b76-279a7ca25712

---

## 0. この文書の使い方（Opusへ）

- 設計は確定済み。この文書の範囲で実装し、判断に迷う点は **15章「CEO確認事項」に追記して止める**（勝手に決めない）
- **禁止**（CLAUDE.md準拠）: `wrangler pages deploy` / `wrangler deploy` の直接実行（`ci-x-cron/scripts/safe-deploy.sh` 経由・CEO承認後のみ）、本番D1の破壊的操作、`git push`（CEO承認後のみ）、APIキー・トークンをチャット/リポジトリ/D1に書くこと、**他アカウントのスクレイピングやフォロワー監視の実装（v3で廃止）**、いいね/フォロー/リプライの自動化
- **既存コード（M1・ローカル完成・未コミット）を壊さない**。`node --test x/linter.test.mjs` 16本と `ci-x-cron` の `npm test` 15本は常に全通過
- 完了報告は**実機の証拠つき**（curlの応答・D1の行・アプリのスクショ・Worker のログ）。無条件のA評価は禁止

### 0.1 現在の状態（2026-09-08）
| 項目 | 状態 |
|---|---|
| 憲法 `guardrails/`（5ファイル）＋ `x/linter.js`（16テスト） | ✅ ローカル完成 |
| `schema-x.sql`（9テーブル） | ✅ **本番D1 `ci_zukou` に適用済み** |
| `functions/api/x/{_auth,candidates,mark,radar,stats}.js` | ✅ ローカル完成・実機検証済み |
| `index.html` 🏮Xタブ | ✅ ローカル完成（:8817で描画確認） |
| `ci-x-cron/`（self/metrics/patterns/radar・OAuth1公式ベクタ検証・15テスト） | ✅ ローカル完成 |
| Pages 環境変数 `X_WRITER_KEY` `X_ADMIN_TOKEN` | ✅ 本番投入済み（値は CEO の `~/.config/himori/tokens.env`） |
| 本番D1 `x_watchlist` | ✅ **空にした**。`x_settings.radar_enabled='0'` |
| git | ⚠️ **未コミット**（main）。他セッションの `git add -A` に巻き込まれないよう、対象ファイルのみ add すること |
| X 開発者アカウント | ✅ CEO取得済み（Read/Write・クレジット）。4キーは CEO が `wrangler secret put` で投入する |

---

## 1. v3 で確定したこと（v2からの変更）

| 項目 | v2 | **v3（確定）** |
|---|---|---|
| 学習データ | 自社＋他アカウント監視（成長レーダー） | **自社データのみ**。他者のスクレイピング・監視はしない。新しい型の発見は**香司の手動登録**（研究タブ） |
| 初期知識 | レーダーの当たり型 | **Threads実測（4.5万人到達）を事前分布**として注入（付録A） |
| 探索 | レーダー任せ | **探索枠を予算化**: 毎日3候補のうち1本は「未検証の型」。Thompson sampling で自然に探索 |
| 評価軸 | いいね／インプ | **推定フォロワー獲得数**（プロフクリック×転換率、日次フォロワー増分で校正）。3万人に直結する唯一の指標 |
| 頭脳層のホスト | Mac Studio の `claude -p` | **Mac Studio 常駐で確定**（2026-09-08 CEO決定）。`daily-engine/run-x.sh` ＋ `com.crystalinsence.himori.plist`（毎朝06:30）。既存Threadsエンジンのパス不具合（`/Users/gaki/`）は修正済み |
| 投稿頻度 | 1日1本 | **1日2本（朝7:30-8:30／夜19:30-21:00）＋人によるリプライ1日15件前後**（2026-09-08 調査で確定。§17参照） |
| 配信 | Phase A 手動 → Phase B 自動 | 変更なし。切替条件 = 1,000人到達 or 3週間経過 |
| 費用 | 月700〜3,500円 | **月200円程度**（Owned Reads のみ） |
| 「毎日確実に改善」の定義 | — | 3章の**日次不変条件**として明文化。ログで検証可能にする |

---

## 2. 思想: 投稿は実験である

「賢くなる」を曖昧にしない。灯守は**毎日の投稿を、事前に宣言した実験として扱う**。

```
投稿 1本 = 実験 1回 = 5つのダイヤルの組み合わせ（レシピ）
  pattern（型）   × hook（1行目の型） × fact（燃料にする一次情報）
  × slot（時間帯） × format（本文のみ／追いリプ／スレッド／写真）
```

- レシピは**投稿前に**確定して記録する（後付けの解釈を禁止する。これが学習を腐らせない唯一の方法）
- 結果（推定フォロワー獲得）を、5つのダイヤルそれぞれの「腕（arm）」に帰属させる
- 明日のレシピは、腕ごとの成績分布から **Thompson sampling** で引く（成績が良い腕ほど選ばれやすく、データが少ない腕も一定確率で試される）
- 学んだことは **学習台帳（ledger）** に毎日1行以上、人が読める日本語で残す。台帳が増えることが「賢くなった」の証拠

### なぜ5次元に分けるか
データが少ない（1日1〜2本）段階でも、型・フック・時間帯を**別々に**学べる。「型Aは弱いがフックBは強い」が分かれば、型Aを捨てずにフックを差し替えられる。1本の投稿から5つの学びを取る設計。

### 自走の3原則
1. **決定は機械、文章はAI、公開は人**。学習と選択（Worker）は決定的なコードで、生成（Claude）は文章だけを書き、公開は人の親指（Phase A）または人の承認（Phase B）
2. **一段でも欠けても翌日に立て直る**。生成が飛べば前日の未使用候補と常備ストックで埋まり、計測が飛べばAPIが履歴を返すので取り戻せる。人が投稿しなければ何も起きない（安全側）
3. **改善は台帳で証明する**。「今日は何を学んだか」が1行も書けない日は、学習の故障として通知する

---

## 3. 「毎日確実に改善」の定義 — 日次不変条件

毎日 03:30 JST の学習ジョブ終了時点で、以下が**すべて**成り立つこと。成り立たなければ `guard_log` に `learn/error` を書き LINE 通知。

| # | 不変条件 | 検証方法 |
|---|---|---|
| I1 | 直近14日の全投稿について、当日分のメトリクススナップショットが存在する | `x_metrics` に `snap_date = today` の行数 ≥ 投稿数 |
| I2 | 自アカウントのフォロワー数が当日記録されている | `x_account_daily` に today の行 |
| I3 | 帰属計算が完了し、7日経過した投稿の観測値が `final` として確定している | `x_observations.status='final'` |
| I4 | 5次元すべての腕の統計が更新されている（更新時刻が today） | `x_arm_stats.updated_at` |
| I5 | 明日のレシピ3本が生成され、制約検査を通っている | `x_recipes` に tomorrow の3行、`constraint_ok=1` |
| I6 | 学習台帳に today の行が ≥1 ある（データがない日は「投稿なし。明日はレシピ#1を」と書く） | `x_ledger` |
| I7 | 転換率 `conv` が再計算されている（分母不足なら既定値0.3と明記） | `x_settings.conv` |

**「改善した」の定義**: I1〜I7 が成立し、かつ (a) いずれかの腕の事後平均が更新された、または (b) 制約により明日のレシピが前日と異なる。(a)(b) いずれも成立しない日は「停滞」として台帳に記録し、3日連続で停滞なら「投稿が足りない／計測が壊れている」を CEO に通知する。

---

## 4. 全体構成

```
📜 憲法 guardrails/           事実表・NG語・禁止主題・文体・勝ち型・一次情報の在庫（fact-inventory）
        │ リンターと生成プロンプトが同じファイルを読む
        ▼
🧠 頭脳層 = Claude クラウドルーティン（毎朝06:30 JST・サブスク枠）
        レシピ3本を読む → 文章を書く → リンター → 「今日の候補」へ（下書きまで。公開はできない）
        週次（日曜20:00）: 台帳と成績を読んで5行のブリーフ
        月次（1日）: 公開アルゴリズム param.rs の重み差分を報告
        ▼
👤 人間層 = アプリ🏮Xタブ（1日2分）＋ 返信5分
        Phase A: 候補を選んでコピペ投稿 → 「投稿した」を記録
        Phase B: スワイプ承認 → 自動配信
        研究タブ: 「いいな」と思った他人の投稿を手で登録（型タグ付け）→ 探索の種
        ▼
⚙️ 24時間層 = Cloudflare Worker ci-x-cron（cron・D1・LLM不要・無料枠）
        03:00 計測（Owned Reads）  03:10 帰属・転換率・腕の更新  03:20 制約検査→明日のレシピ3本  03:30 台帳・不変条件検証
        Phase B: 配信（承認済みのみ・学習した時間帯・ジッター・上限・ブレーキ）
```

**Mac Studio は使わない**（頭脳層はクラウドルーティン）。ただし `daily-engine/` の Threads エンジンと同じ形で動かしたい場合に備え、生成ルーティンは「HTTP APIだけを叩く」設計にし、どこで動いても同じにする。

置き場所: `gakikocamp/ci-threads` に同居（アプリ=`index.html` 🏮タブ、API=`functions/api/x/`、Worker=`ci-x-cron/`、憲法=`guardrails/`、リンター=`x/`）。D1 は `ci_zukou` に相乗り。

---

## 5. データモデル（v3追加分）

既存9テーブル（`schema-x.sql`）は維持。**`x_watchlist` / `x_radar` / `x_radar_posts` は使わない**（削除もしない。空のまま）。以下を `schema-x-v3.sql` として追加し、ローカル→本番（CEO承認後）に適用する。

```sql
-- 実験の記録: 投稿1本のレシピ（投稿前に確定）と観測値
ALTER TABLE x_queue ADD COLUMN recipe_id TEXT;          -- x_recipes.id
ALTER TABLE x_queue ADD COLUMN hook TEXT;               -- 1行目の型
ALTER TABLE x_queue ADD COLUMN fact_ids TEXT;           -- JSON配列（fact-inventory の id）
ALTER TABLE x_queue ADD COLUMN slot TEXT;               -- morning | noon | evening
ALTER TABLE x_queue ADD COLUMN format TEXT;             -- single | reply | thread | photo
ALTER TABLE x_queue ADD COLUMN is_exploration INTEGER DEFAULT 0;

-- 明日の実験レシピ（Workerが決定的に生成。Claudeはこれを読んで文章を書く）
CREATE TABLE IF NOT EXISTS x_recipes (
  id TEXT PRIMARY KEY,               -- 'YYYY-MM-DD-1' .. '-3'
  date TEXT NOT NULL,
  rank INTEGER NOT NULL,             -- 1,2 = 活用 / 3 = 探索
  pattern TEXT NOT NULL,
  hook TEXT NOT NULL,
  fact_ids TEXT NOT NULL,            -- JSON配列 1〜2件
  slot TEXT NOT NULL,
  format TEXT NOT NULL,
  is_exploration INTEGER DEFAULT 0,
  rationale TEXT,                    -- なぜこの組合せか（腕の事後平均・制約）
  constraint_ok INTEGER DEFAULT 0,
  used_queue_id TEXT,                -- 生成後に x_queue.id
  created_at INTEGER
);
CREATE INDEX IF NOT EXISTS idx_x_recipes_date ON x_recipes(date);

-- 5次元の腕の統計（Welford: n, mean, m2）。prior は Threads 事前分布（付録A）
CREATE TABLE IF NOT EXISTS x_arm_stats (
  dimension TEXT NOT NULL,           -- pattern | hook | fact | slot | format
  arm TEXT NOT NULL,
  n INTEGER DEFAULT 0,
  mean REAL DEFAULT 0,
  m2 REAL DEFAULT 0,
  prior_mean REAL,                   -- global_mean × multiplier
  prior_n REAL DEFAULT 2,
  last_tried TEXT,                   -- 'YYYY-MM-DD'
  updated_at INTEGER,
  PRIMARY KEY (dimension, arm)
);

-- 投稿ごとの観測値（帰属計算の結果）。provisional(48h) → final(7d)
CREATE TABLE IF NOT EXISTS x_observations (
  queue_id TEXT PRIMARY KEY,
  tweet_id TEXT,
  posted_at INTEGER,
  status TEXT NOT NULL,              -- provisional | final
  impressions INTEGER,
  profile_clicks INTEGER,
  replies INTEGER, quotes INTEGER, reposts INTEGER, likes INTEGER, bookmarks INTEGER,
  est_follows_clicks REAL,           -- profile_clicks × conv
  est_follows_direct REAL,           -- 日次フォロワー増分のインプ按分
  est_follows REAL,                  -- 0.5×clicks + 0.5×direct（観測値）
  engagement_score REAL,             -- 5×replies + 5×quotes + 1×reposts + 0.5×likes + 2×bookmarks（診断用）
  updated_at INTEGER
);

-- 学習台帳（人が読む。毎日≥1行）
CREATE TABLE IF NOT EXISTS x_ledger (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT NOT NULL,
  kind TEXT NOT NULL,                -- learn | stagnate | alert | weekly | monthly | human
  text TEXT NOT NULL,                -- 日本語1〜2文
  evidence TEXT,                     -- JSON（腕名・n・mean の変化など）
  created_at INTEGER
);
CREATE INDEX IF NOT EXISTS idx_x_ledger_date ON x_ledger(date);

-- 一次情報（燃料）の使用履歴。定義は guardrails/fact-inventory.json（静的）、使用は D1
CREATE TABLE IF NOT EXISTS x_fact_usage (
  fact_id TEXT NOT NULL,
  queue_id TEXT NOT NULL,
  used_date TEXT NOT NULL,
  PRIMARY KEY (fact_id, queue_id)
);

-- 香司の手動研究登録（他人の投稿の「型」だけを学ぶ。文面の再利用は禁止）
CREATE TABLE IF NOT EXISTS x_research (
  id TEXT PRIMARY KEY,
  url TEXT,
  text TEXT,                         -- 内部学習用。生成の類似度検査で「模倣禁止」の対象にもなる
  handle TEXT,
  pattern_tags TEXT,                 -- JSON配列（既存15型 or 新規型名）
  why TEXT,                          -- なぜ良いと思ったか（香司の言葉）
  metrics_json TEXT,
  added_by TEXT DEFAULT 'human',
  added_at INTEGER
);

-- 常備ストック（生成が飛んだ日の保険。承認済み・季節非依存の5本）
-- x_queue の status='stock' として保持（ステートに 'stock' を追加: stock → posted のみ許可）

INSERT OR IGNORE INTO x_settings (key, value, updated_at) VALUES ('conv', '0.3', 0);
INSERT OR IGNORE INTO x_settings (key, value, updated_at) VALUES ('cadence_per_day', '1', 0);   -- 1 or 2
INSERT OR IGNORE INTO x_settings (key, value, updated_at) VALUES ('exploration_min_per_week', '2', 0);
INSERT OR IGNORE INTO x_settings (key, value, updated_at) VALUES ('global_mean', '1.0', 0);
```

`guardrails/fact-inventory.json`（新規・静的）: CIの一次情報を燃料として列挙。各 `{id, text, category, source, strength}`。初期は `fact-table.json` の `numbers_allowed` と `proven-patterns.md` の固有名詞（椨・水車・八女杉・屋久杉・山口県産水晶・6つの不使用・3代続く工場・あと2人・あと5年・15分）から起こす。**ここに無い事実は生成に使えない**（リンターが止める）。

---

## 6. 計測ジョブ（Worker・03:00 JST）— 既存 `metrics.js`/`self.js` を拡張

- 取得: `/2/users/me`（フォロワー数）→ `x_account_daily`。`/2/users/:id/tweets`（直近14日・`non_public_metrics`/`organic_metrics`）→ `x_metrics(tweet_id, snap_date)`
- **Owned Reads のみ**（1件0.16円）。他アカウントの読み取りコードは呼ばない（`radar.js` は `x_settings.radar_enabled='0'` で常時スキップ。将来も使わない前提でよい）
- 突合: `x_queue` の `tweet_id` 空の行に、本文先頭30文字一致で `tweet_id` を埋める（既存）。**加えて投稿時刻から `slot` を自動判定**して埋める（Phase A は本人が投稿時刻を選ぶため）
- 失敗時: 401/403 → 即停止・`guard_log(error)`・LINE通知（凍結予兆で叩き続けない）。429 → その日はスキップ。**翌日は必ず14日分を取り直す**のでデータは失われない

---

## 7. 学習ジョブ（Worker・03:10〜03:30 JST）— 新規 `src/jobs/learn.js`

### 7.1 帰属（1投稿 → 推定フォロワー獲得数）
投稿 p（posted_at = t）について:
- `impressions_p, profile_clicks_p, …` = 最新スナップショット
- **clicks経路**: `est_follows_clicks = profile_clicks_p × conv`
- **direct経路**: 日 d ∈ [t日, t+2日] の各日で `Δfollowers⁺(d) = max(0, followers(d) − followers(d−1))` を、その日にアクティブな投稿（過去48h以内に投稿）の**インプレッション按分**で配る: `share_p(d) = impressions_p / Σ impressions_active(d)`。`est_follows_direct = Σ_d Δfollowers⁺(d) × share_p(d)`
- **観測値**: `est_follows = 0.5 × clicks + 0.5 × direct`
- `status`: 投稿から48h経過で `provisional`（重み0.5で腕に反映）、7日経過で `final`（provisional を取り消して重み1.0で反映）
- `engagement_score = 5×replies + 5×quotes + 1×reposts + 0.5×likes + 2×bookmarks`（診断・タイブレーク用。X公開重み準拠。ブックマークの重みは未公開のため2で仮置き）

### 7.2 転換率 conv の校正
`conv = Σ Δfollowers⁺(直近14日) ÷ Σ profile_clicks(直近14日に投稿された分)`。分母 < 50 なら既定 0.3。範囲 [0.05, 0.6] にクランプ。`x_settings.conv` に保存し台帳に記録。

### 7.3 腕の更新（5次元）
final になった観測値 o について、その投稿のレシピの5つの腕（pattern/hook/fact×k/slot/format）それぞれに Welford 更新（n, mean, m2）。`x_arm_stats` に無い腕は付録Aの multiplier から `prior_mean = global_mean × multiplier, prior_n = 2` で作成。`global_mean` は全 final 観測の平均（初期1.0）。

### 7.4 明日のレシピ（Thompson sampling）
各次元で: 事後平均 `μ = (prior_n×prior_mean + n×mean)/(prior_n+n)`、分散 `σ² = max(m2/max(n−1,1), 0.25×global_mean²)`、サンプル `θ ~ N(μ, σ²/(prior_n+n))`。θ 最大の腕を選ぶ。
- **レシピ#1・#2（活用）**: 各次元を独立にサンプル（#2 は pattern を #1 と変える）
- **レシピ#3（探索）**: `n < 2` の pattern×hook のうち `last_tried` が最も古いものを優先（14日以内に必ず一巡）。無ければ通常サンプル。`is_exploration=1`
- `cadence_per_day=1` の日も3本作る（人が選ぶ余地＝Phase A の価値）

### 7.5 制約検査（proven-patterns.md §5 と憲法に従う。違反なら再サンプル、5回で諦めて台帳に記録）
- 数字絶滅型は**週1本まで**／お礼は**月1本まで**／導線（CTA）は**週2本まで**／**同じ pattern を2日連続で使わない**
- **同じ fact を14日以内に再使用しない**（`x_fact_usage`）
- 死に型（説明分析型・フォロー乞い単体・転身自己紹介の単発）は腕として存在させない
- `mode=safety`（災害検知中）は「安否と事実」型のみ、CTA・タグなし
- 探索枠は週 `exploration_min_per_week` 本以上を保証（日曜に不足していれば月曜のレシピ#1も探索にする）

### 7.6 台帳（毎日≥1行・日本語）
自動生成テンプレ（例）:
- 「型『問いかけ二択』の推定フォロー平均 1.8→2.4（n=4, final）。明日のレシピ#1に採用」
- 「夜枠（21-24時）が朝枠を3週連続で上回る（2.1 vs 1.4）。Phase B の配信は夜枠を優先」
- 「事実『椨粉職人あと2人』は9/1に使用、14日ルールで9/15まで休止」
- 「転換率 conv を 0.30→0.22 に校正（分母 312 クリック）」
- 停滞: 「新規観測なし（投稿0本）。明日はレシピ#1『秘密予告×椨の粉×夜』を投稿してください」
- 3章の不変条件が崩れたら `kind='alert'`

---

## 8. 生成ルーティン（Claude・毎朝06:30 JST）— 新規 `x/routine-prompt.md`

**実行基盤**: Claude Code のスケジュール機能（`/schedule`・クラウド定期実行）。**APIキー不要・従量課金なし**（[[feedback-no-auto-api]] 準拠）。プロンプト本文はリポジトリの `x/routine-prompt.md` を単一の真実源とし、ルーティンはそれを読んで実行する。

### 8.1 入力（すべて HTTP・`x-writer-key`）
1. `GET /api/x/recipes?date=today` — レシピ3本
2. `GET /api/x/facts` — `fact-inventory.json` ＋ 使用履歴（14日以内に使った fact は除外済み）
3. `GET /api/x/stats` — 腕の成績・直近の観測・conv
4. `GET /api/x/ledger?days=7` — 直近の学び
5. `GET /api/x/research?limit=20` — 香司の手動登録（型だけ参照。文面は模倣禁止）
6. 憲法（`guardrails/*.md,json`）— リポジトリから直接読む
7. `GET /api/x/candidates?date=yesterday` — 昨日の未使用候補（繰り越し判断用）

### 8.2 出力（`POST /api/x/candidates`・3本）
各候補は**レシピを厳守**: `{recipe_id, pattern, hook, fact_ids, slot, format, body, reply?, thread?, tag?, confidence, rationale, is_exploration}`。
- 本文は `style-rules.md` 準拠（280単位・URLなし・一人称「私」・記号なし・締めは余韻か問い）
- fact は `fact_ids` のもの**だけ**を使う（在庫外の事実・数字・固有名詞は書かない）
- `rationale` は人が読む1文（「型『秘密予告』は n=3 で平均2.4。今日は夜枠で試す」）
- サーバー側リンターで BLOCK された候補は `killed` で保存される。ルーティンは結果を読み、BLOCK が2本以上なら**同じレシピで書き直して再投入**（最大2回）

### 8.3 日次ブリーフ（`POST /api/x/brief`・3行）
「昨日: 投稿n本・推定フォロー+x・フォロワー N（±d）」「学び: 台帳から1行」「今日: レシピ#1を推す理由」。アプリ🏮タブ上部に表示。

### 8.4 失敗時
- ルーティンが動かなかった／候補が全滅: アプリは**昨日の未使用候補**＋**常備ストック5本**（`status='stock'`）を表示するので、その日の投稿は途切れない
- 2日連続で候補が届かなければ Worker が LINE 通知（「生成が止まっています」）

---

## 9. 人間の役割とアプリ（🏮Xタブの拡張）

| 役割 | 頻度 | UI |
|---|---|---|
| 候補から1本選んで投稿（Phase A）／承認（Phase B） | 毎朝2分 | 既存の「今日の候補」＋ブリーフ3行を上部に。**レシピの5ダイヤルをチップ表示**（型・フック・燃料・時間帯・形式・🧪探索） |
| 「投稿した」を記録（投稿URL貼付で即突合） | 投稿直後 | 既存 |
| リプ返信 | 随時5分 | アプリ外（Xアプリ）。AIは返信案を用意しない（v3では省く。人の言葉だけ） |
| 研究登録: 「いいな」と思った投稿のURL＋本文＋型タグ＋なぜ | 気づいた時 | **研究タブ（X）**を追加。Threads研究タブと同じ入力項目 |
| 週次ブリーフを読む | 日曜夜5分 | 🏮タブに「今週の学び」カード |
| 常備ストックの承認 | 月1 | 生成が「ストック候補」を出し、人が5本を選んで `stock` にする |

**投稿時刻の指針（Phase A）**: 候補の `slot` に従う。朝=7:30〜8:30／昼=12:00〜13:00／夜=21:30〜22:30。ずらすのは±30分まで（学習の時間帯帰属を壊さないため）。

---

## 10. Phase B — 自動配信とブレーキ（v2継承・時間帯だけ学習結果を使う）

- 承認済みキュー（`approved`）のみ配信。**`x_arm_stats(dimension='slot')` の事後平均が最も高い枠**に、±30分ジッターで配信
- 上限: 1日≤`cadence_per_day`（最大3）・間隔≥3h・配信直前にリンター再検査
- ブレーキ: 緊急停止（`paused=1`）／気象庁フィード（震度5強以上・特別警報 → 48h停止・`mode=safety`）／API 401/403/429 → 即停止・通知／キュー枯渇時に自動生成で埋めない
- 切替条件: **1,000人到達 or Phase A 開始から3週間**。切替は `x_settings.phase='B'`
- エンゲージ自動化（いいね・フォロー・リプ）のコードは**存在させない**

---

## 11. 週次・月次ルーティン

- **週次（日曜20:00 JST・Claude）**: 台帳7日分＋腕の成績＋観測を読み、5行のブリーフ（続ける／やめる／来週試す探索枠2本／フォロワー進捗 vs マイルストーン／CEOに聞くこと）→ `POST /api/x/brief?kind=weekly`。**型の優劣は n≥5 になるまで断定しない**（Threads立ち上げ時の鉄則）
- **月次（1日07:00 JST・Claude）**: `https://raw.githubusercontent.com/xai-org/x-algorithm/main/home-mixer/params/param.rs` を取得し、前月保存分と重みを比較。変化があれば `engagement_score` の係数案と台帳 `kind='monthly'` を書く（係数変更は CEO 承認後に `x_settings` へ）
- **マイルストーン監視（Worker・毎日）**: 1,000/5,000/30,000 到達と、週次フォロワー増分の移動平均を `x_ledger` に記録。2週連続で週増分が前週比マイナスなら `alert`

---

## 12. 自走の保証 — 故障時の挙動一覧

| 故障 | 挙動 | データ損失 |
|---|---|---|
| Claude ルーティンが動かない | 昨日の未使用候補＋常備ストックを表示。2日連続で通知 | なし |
| X API 認証切れ／制限 | 計測停止・通知。翌日14日分を取り直す | なし |
| 人が投稿しない | 何も起きない。台帳に「投稿なし」。3日連続で通知 | なし |
| D1 書き込み失敗 | ジョブは独立 try/catch。翌日再実行で冪等（PRIMARY KEY upsert） | なし |
| リンターが全候補をBLOCK | ルーティンが同レシピで最大2回書き直し。それでもダメなら昨日の候補＋ストック | なし |
| 災害 | `mode=safety`。配信停止48h（Phase B）。生成は安否型のみ | — |
| 不変条件 I1〜I7 の崩れ | `guard_log(error)`＋LINE通知。人が原因を見る | — |

---

## 13. API 契約（追加分・既存は維持）

| メソッド | パス | 認証 | 用途 |
|---|---|---|---|
| GET | `/api/x/recipes?date=` | writer | レシピ3本 |
| GET | `/api/x/facts` | writer | 燃料在庫（14日以内使用分は `available=false`） |
| GET | `/api/x/ledger?days=` | 公開 | 台帳 |
| GET | `/api/x/brief?kind=daily\|weekly&date=` | 公開 | ブリーフ |
| POST | `/api/x/brief` | writer | ブリーフ保存 |
| GET/POST | `/api/x/research` | GET公開 / POST admin | 手動研究登録 |
| POST | `/api/x/settings` | admin | `paused` `phase` `cadence_per_day` |
| POST | `/api/x/mark` | admin | 既存＋ `stock→posted` 遷移を追加 |
| POST | `/api/x/candidates` | writer | 既存＋レシピ項目（`recipe_id/hook/fact_ids/slot/format/is_exploration`）を保存し `x_fact_usage` に記録 |

ステート: `draft → approved → posted`、`draft → killed`、`stock → posted`。逆行不可（既存）。

---

## 14. 実装順序と受け入れ基準

| M | 内容 | 受け入れ基準（実機） |
|---|---|---|
| **M1′ 整地** | v3スキーマ適用（ローカル→本番はCEO承認後）／`fact-inventory.json` 作成／`radar.js` を `radar_enabled` で恒久スキップ／既存テストを維持／**commit → push（CEO承認）→ Pages 配置**／Worker を `safe-deploy.sh` で DRY_RUN=1 配置／CEO が4キーを `secret put` | `ci-threads.pages.dev` に🏮タブ表示・`/api/x/candidates` がJSONを返す・Worker `/health` 200・`POST /run?job=self` で `x_account_daily` に today の行 |
| **M2 学習エンジン** | `learn.js`（帰属・conv・腕更新・Thompson・制約・レシピ・台帳・不変条件） | 合成データ30投稿のユニットテストで、①帰属の合計が Δfollowers を超えない ②制約違反レシピが0 ③探索枠が週2本以上 ④不変条件が全て検証される。本番で3日連続 I1〜I7 成立 |
| **M3 生成ルーティン** | `x/routine-prompt.md`・`/schedule` 登録（06:30 JST）・`facts`/`recipes`/`ledger`/`brief` API・ストック機構 | 3日連続で06:30に候補3本＋ブリーフが届く。BLOCK候補は `killed` で監査に残る。助走週14本は全通過（回帰） |
| **M4 アプリ＆週次** | 🏮タブにレシピチップ・ブリーフ・台帳・研究タブ(X)・ストック承認／週次・月次ルーティン | スマホ実機で候補→投稿→記録→翌朝の観測反映まで1周。日曜にブリーフが出る |
| **M5 Phase B** | 承認箱・配信 Worker（ドライラン48h→実投稿1本）・ブレーキ | ドライラン誤配信0・実投稿 HTTP 201・緊急停止が効く・災害停止のテスト発火 |

**稼働の定義**: M1′〜M4 が揃い、**14日連続で不変条件 I1〜I7 が成立**し、台帳に14行以上の「学び」があること。ここで初めて「毎日改善するシステム」と報告する。

---

## 15. 運用ランブック（順番どおり・CEO承認済み範囲）

1. `cd threads-app && git add guardrails x schema-x.sql schema-x-v3.sql functions/api/x functions/_middleware.js index.html .gitignore README-x.md ci-x-cron 灯守-実装指示書_v3_2026-09-08.md` → `git commit`（本文末尾に `Co-Authored-By: Claude Opus <noreply@anthropic.com>`）→ **CEO に push 承認を取る** → `git push`（Pages 自動配置）
2. `npx wrangler d1 execute ci_zukou --remote --file=schema-x-v3.sql`（CEO承認後）
3. `cd ci-x-cron && bash scripts/safe-deploy.sh`（DRY_RUN=1）→ `printf '%s' "$X_CRON_TOKEN" | npx wrangler secret put X_CRON_TOKEN`（値は `~/.config/himori/tokens.env`）
4. **CEO**: `npx wrangler secret put X_API_KEY` / `X_API_SECRET` / `X_ACCESS_TOKEN` / `X_ACCESS_SECRET`
5. 検証: `/health`、`POST /run?job=self`、`x_account_daily` の行
6. 翌日 `DRY_RUN="0"` に変更して再配置 → 03:00 以降に `x_metrics` の行
7. アプリ🏮タブ → 🔑 に `X_ADMIN_TOKEN`（CEO のiPhone）
8. `/schedule` で生成ルーティン（06:30 JST）を登録（M3）

---

## 16. CEO確認事項 — すべて解決済み（2026-09-08）

1. ~~頭脳層のホスト~~ → **Mac Studio 常駐で確定**
2. ~~投稿頻度~~ → **1日2本＋リプライ15件/日**（§17）
3. ~~通知チャネル~~ → **LINE**（「LINEでもなんでもOK」）

## 17. 投稿頻度の根拠（2026-09-08 調査）

**結論: オリジナル投稿1日2本（朝7:30-8:30／夜19:30-21:00）＋香司によるリプライ1日15件前後。**

| 根拠 | 内容 |
|---|---|
| 上限は問題にならない | 2026年5月から未認証アカウントは**オリジナル50件/日・リプライ200件/日**（従来2,400件から縮小）。2本は全く問題にならない水準。心配すべきは上限ではなく「宣伝臭」「同じ構文の繰り返し」によるミュート |
| 2026年アルゴリズムの論調 | 「1日◯投稿が最適」という明確な推奨値は**業界記事にも存在しない**。Grok/Phoenix ベースの刷新以降、評価軸が本数からエンゲージメント予測に移ったため。共通するのは「量より質」 |
| **リプライが最大のレバレッジ** | **相互フォロー相手へのリプライは +15 が加算され、通常5.0 → 20.0（4倍）**。この加算は**リプライにのみ適用され、オリジナル投稿には適用されない**。フォロワー8人から伸ばす段階では、連投よりリプライのほうが効く |
| 2本にする理由 | フォロワー8人では1本では露出機会が少なすぎ、5本以上は「量より質」の論調とミュートリスクに反する。2本が折衷点 |
| 時間帯 | 朝の通勤帯と夜の可処分時間帯。香の商品特性（朝の一服・夜の切り替え）とも合う |

**未確認だったこと（正直に）**: @shakunone・@houseiya_moto・saito_ham・菊水産業の**具体的な投稿頻度データは見つからなかった**。「houseiya_moto が18日でフォロワー2万増」という以前の記述も**裏付けが取れなかった**ので、根拠として使わない。

**運用**: 2〜4週間ごとにインプレッションとフォロワー増加率を見て、`cadence_per_day` を1〜3の範囲で調整する。

## 18. 実装済み（2026-09-08・ローカル検証まで完了）

| 部品 | 状態 | 検証 |
|---|---|---|
| `schema-x-v3.sql`（x_recipes/x_arm_stats/x_observations/x_ledger/x_fact_usage/x_research/x_brief/x_health＋x_queueへレシピ列） | ✅ | ローカルD1適用済み。**本番は未適用（CEO承認待ち）** |
| `guardrails/fact-inventory.json`（燃料16件） | ✅ | `/api/x/facts` が16件・available 8件を返す |
| `ci-x-cron/src/learn-core.js`（純粋関数: 決定的乱数・Welford・帰属・Thompson・制約・台帳・不変条件） | ✅ | **39テスト全通過** |
| `ci-x-cron/src/priors.js`（付録Aの事前分布） | ✅ | |
| `ci-x-cron/src/jobs/learn.js`（DB結合・冪等） | ✅ | 合成データ30投稿で**不変条件 I1〜I7 が全成立**。型の強さの序列を正しく再現（数字絶滅型8.69＞問いかけ二択8.21＞…＞舞台裏日常3.06） |
| `radar.js` の恒久停止（`radar_enabled=0`） | ✅ | 本番D1の `x_watchlist` は空にした |
| API `/api/x/{recipes,facts,ledger,brief}` ＋ candidates/mark の拡張（レシピ項目・燃料使用記録・stock遷移） | ✅ | ローカル実機で200・JSON確認 |
| アプリ🏮Xタブ v3（ブリーフ・不変条件ストリーク・レシピのダイヤル・🧪探索チップ・学習台帳・明日のレシピ／レーダー欄は撤去） | ✅ | ローカル実機で描画確認 |
| `x/routine-prompt.md`（生成プロンプトの単一真実源） | ✅ | |
| `daily-engine/run-x.sh` ＋ `com.crystalinsence.himori.plist`（毎朝06:30） | ✅ | plutil検証OK・実行権限付与済み。**Mac Studioへの登録は未実施** |
| Threadsエンジンのパス修正（`/Users/gaki/` → `/Users/gakipro/`） | ✅ | plutil OK |

**残り**: ①本番D1へ v3スキーマ適用 ②`seed-settings.mjs --remote` ③commit→push（Pages配置）④Worker配置＋秘密投入（CEO）⑤Mac Studio に launchd 登録＋Claude Code 再ログイン ⑥週次・月次ルーティン ⑦Phase B（M5）

---

## 付録A. Threads 事前分布（腕の初期 multiplier）

出典: @crystal_insence 実測（2026-07-24棚卸し）と国産バズ研究。`prior_mean = global_mean × multiplier`、`prior_n = 2`。

| 次元 | 腕 | multiplier | 根拠・制約 |
|---|---|---|---|
| pattern | 数字絶滅型 | 1.6 | 8.7万・3,183。**週1本まで** |
| pattern | 懇願＋写真型 | 1.4 | 2.1万（固定投稿向き） |
| pattern | 問いかけ二択 | 1.2 | 5,032。返信+5 |
| pattern | そっといいね型 | 1.2 | 9,919。コモディティ化・**月1本まで** |
| pattern | 真実暴露型（秘密予告） | 1.1 | 3,291。共有+20 |
| pattern | 利他応援型 | 1.1 | 他社3.0万・CI未使用 → **探索優先** |
| pattern | 廃材×舞台裏型 | 1.1 | 他社9,419 |
| pattern | 挙手集計型 | 1.1 | 他社1.2万 |
| pattern | 誤解訂正型 | 1.0 | 1,101 |
| pattern | リフレーム型 | 1.0 | 他社観測・未使用 |
| pattern | 意外な数字ゼロ開始型 | 1.0 | 事実の日だけ |
| pattern | 保存版の知恵 | 1.0 | X の共有+20 を狙う |
| pattern | 舞台裏・日常 | 0.9 | |
| pattern | 仲間募集 | 0.9 | |
| pattern | お礼 | 0.5 | **月1本まで** |
| — | 説明分析型／フォロー乞い単体／転身自己紹介 | — | **腕として作らない**（死に型） |
| hook | 衝撃数字 | 1.4 | CIバズ法則 |
| hook | 懇願 | 1.3 | |
| hook | 秘密予告 | 1.2 | |
| hook | 問いかけ | 1.2 | |
| hook | 現場の一文 | 1.0 | |
| hook | 告白 | 1.0 | |
| hook | 逆説 | 1.0 | |
| slot | evening（21-24） | 1.1 | Threads研究: 共感・暮らし系は夜有利 |
| slot | morning（7-9） | 1.0 | |
| slot | noon（12-14） | 0.9 | |
| format | single | 1.0 | |
| format | reply（追いリプ付き） | 1.1 | 会話誘発 |
| format | thread | 1.0 | 滞在時間 |
| format | photo | 1.0 | 現場写真がある時のみ |
| fact | （fact-inventory の各id） | 1.0 | 使用のたび14日休止 |

## 付録B. 生成プロンプト骨子（`x/routine-prompt.md` の章立て）
1. あなたは香司・柴垣道宏（しばがき どうこう）の編集者。著者は本人。憲法（guardrails/）を全文読む
2. `GET recipes` の3本を**厳守**（型・フック・燃料・時間帯・形式を変えない）
3. `GET facts` にある事実だけを使う。数字・産地・固有名詞を在庫外から足さない
4. `GET stats`/`ledger`/`research` は「なぜ今日これか」の根拠に使う（research の文面は模倣禁止・型のみ）
5. 各候補: 本文（280単位以内・1行目はフックの型・締めは余韻か問い）＋必要なら追いリプ／スレッド＋rationale 1文
6. `POST candidates` → 応答の BLOCK を読み、2本以上なら同レシピで書き直し（最大2回）
7. `POST brief`（3行）。投稿・承認・削除はしない

## 付録C. 用語
- **腕（arm）**: 学習対象の選択肢（型・フック・燃料・時間帯・形式の各値）
- **レシピ**: 投稿1本の実験設計（5つの腕の組合せ）。投稿前に確定
- **観測値**: 投稿1本の推定フォロワー獲得数（provisional 48h → final 7d）
- **台帳（ledger）**: 毎日の学びの日本語ログ。賢くなった証拠
- **常備ストック**: 生成が飛んだ日の保険となる承認済み5本
